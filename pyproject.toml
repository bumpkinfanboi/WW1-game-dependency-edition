[project]
name = "ihatemyoldww1game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "keyboard==0.13.5",
    "pip==24.0",
    "uv==0.12.13",
]

[project.scripts]
ihatemyoldww1game = "ihatemyoldww1game:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
